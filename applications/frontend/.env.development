# API Configuration
# Leave empty for relative URLs (works with ALB in production)
# For local development with backend running on different port, set to http://localhost:8080
VITE_API_URL=http://localhost:8080

# WebSocket Configuration
# For local development
VITE_WS_URL=http://localhost:8080

# Environment
VITE_ENV=development
