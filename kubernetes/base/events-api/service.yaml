apiVersion: v1
kind: Service
metadata:
  name: events-api-service
  namespace: campus-events
  labels:
    app: events-api
    component: api
spec:
  type: NodePort
  selector:
    app: events-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
